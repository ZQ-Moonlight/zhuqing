---
import BaseLayout from "../layouts/BaseLayout.astro";
import { withBase } from "../lib/paths";
---

<BaseLayout
    title="祝庆 Zhu Qing | 影视后期与 Unreal Engine 开发"
    description="祝庆的个人作品集首页，聚焦影视后期、UE5、虚拟制片、MetaHuman 与视觉设计。"
    activePath="/"
>
    <main class="home-hero">
        <div class="home-grid">
            <section class="profile-col" data-reveal>
                <div class="cinematic-frame">
                    <img src={withBase("/images/home/zq.jpg")} alt="祝庆 Zhu Qing cinematic portfolio still" loading="eager" fetchpriority="high" />
                </div>

                <p class="eyebrow">影视技术 / UE Development</p>
                <h1>
                    <span>祝庆</span>
                    <small>Zhu Qing</small>
                </h1>
                <div class="role-tags" aria-label="Profile tags">
                    <span>BFA Student</span>
                    <span class="divider"></span>
                    <span>MetaHuman</span>
                    <span class="divider"></span>
                    <span>Post-Production</span>
                </div>
            </section>

            <section class="bio-col" data-reveal>
                <div class="language-tabs" role="tablist" aria-label="Language">
                    <button class="active" type="button" data-lang-button="cn" aria-controls="content-cn">中文</button>
                    <button type="button" data-lang-button="en" aria-controls="content-en">EN</button>
                </div>

                <div id="content-cn" class="lang-content active" data-lang-panel="cn">
                    <p class="lead">
                        我是祝庆，目前正在北京电影学院学习影视技术。
                    </p>
                    <p>
                        我一直很着迷于影视后期、虚幻引擎开发和视觉设计之间那个交叉的地带：技术怎样才能更好地服务于故事，又怎样能催生出新的视觉语言。
                    </p>
                    <p>
                        我在学校 ISFVF 电影节当了三年的字幕组组长，无论是内嵌字幕、XML 外挂字幕，还是自媒体平台的各种发行版本，我都很熟悉。另外，利用学校的放映机资源，我也掌握了 DCP 的制作和放映。能亲手完成电影院放映的最后一环，真的很令人兴奋。
                    </p>
                    <p>
                        在 UE5 的自学中，我复刻过《底特律：化身为人》的一个章节，也熟悉 nDisplay 虚拟拍摄 LED 背景墙的流程。同时，借助系里的光台资产，我研究了如何把真人复刻成 MetaHuman 数字人。我还做了一个纯原创的作品，结合高斯泼溅与数字人技术，拿到了 2025 年中国创新影像大赛数字人赛道的奖项。游戏技术让我着迷，尤其是过场动画的设计。
                    </p>
                    <p>
                        在剪辑和调色方面，我有幸参与了一些短片和商业项目的后期制作，合作方包括小红书、国家地理。目前我也在尝试将 3D 立体拍摄、CG 制作与调色流程结合起来：这个过程很有挑战，也让我兴奋。
                    </p>
                    <div class="contact-box">
                        <span>如果你对研究项目、电影制作或技术开发感兴趣，或者只是单纯想聊聊历史与电影之间有意思的事，欢迎通过邮件联系我。我也在寻找影视后期或互动式电影游戏开发方向的工作/实习机会，希望能用我的技能为团队创造价值。</span>
                        <div class="contact-actions">
                            <a href="mailto:zq_eugene@qq.com">zq_eugene@qq.com</a>
                            <button class="message-button" type="button" data-message-open>留言给我</button>
                        </div>
                    </div>
                </div>

                <div id="content-en" class="lang-content" data-lang-panel="en" hidden>
                    <p class="lead">
                        I am Zhu Qing, currently studying Film and Television Technology at Beijing Film Academy.
                    </p>
                    <p>
                        I have long been fascinated by the space where post-production, Unreal Engine development, and visual design overlap: how technology can better serve story, and how it can give rise to new visual languages.
                    </p>
                    <p>
                        For three years, I led the subtitle team at my school's ISFVF film festival. I am comfortable with embedded subtitles, XML sidecar subtitles, and the many delivery versions required by social media platforms. With access to the academy's projection facilities, I have also learned DCP creation and screening. Completing the final step before a film reaches the cinema screen still feels genuinely exciting.
                    </p>
                    <p>
                        In my self-directed UE5 practice, I recreated a chapter from <em>Detroit: Become Human</em> and became familiar with nDisplay workflows for LED virtual production backgrounds. Using light-stage assets, I also researched how to reconstruct a real person as a MetaHuman digital human. I created an original work combining Gaussian Splatting and digital-human techniques, which received an award in the Digital Human category at the 2025 China Innovative Image Competition. Game technology fascinates me, especially the design of cinematics.
                    </p>
                    <p>
                        As an editor and colorist, I have been lucky to join post-production on short films and commercial projects, with collaborators including Xiaohongshu and National Geographic. I am now experimenting with combining stereoscopic 3D shooting, CG production, and color workflows: a demanding process, and one that keeps me excited.
                    </p>
                    <div class="contact-box">
                        <span>If you are interested in research projects, filmmaking, or technical development, or if you simply want to talk about the strange and fascinating ties between history and cinema, feel free to reach out. I am also looking for work or internship opportunities in post-production and interactive film/game development, where I hope my skills can create value for a team.</span>
                        <div class="contact-actions">
                            <a href="mailto:zq_eugene@qq.com">zq_eugene@qq.com</a>
                            <button class="message-button" type="button" data-message-open>Leave a note</button>
                        </div>
                    </div>
                </div>
            </section>
        </div>
    </main>

    <div class="message-modal" data-message-modal hidden>
        <div class="message-modal__backdrop" data-message-close></div>
        <section class="message-dialog" role="dialog" aria-modal="true" aria-labelledby="message-title" data-message-dialog>
            <button class="message-close" type="button" aria-label="关闭留言窗口" data-message-close>×</button>
            <span class="eyebrow">Email Message</span>
            <h2 id="message-title">给我留言</h2>
            <p>写下想聊的内容和联系方式，提交后会用你的邮件客户端发给我。</p>
            <form class="message-form" data-message-form>
                <label>
                    <span>联系方式</span>
                    <input name="contact" type="text" autocomplete="email" placeholder="邮箱 / 微信 / 电话" required />
                </label>
                <label>
                    <span>留言</span>
                    <textarea name="message" rows="6" placeholder="想合作的项目、问题，或者只是打个招呼。" required></textarea>
                </label>
                <button type="submit">发送到邮箱</button>
            </form>
            <div class="message-success" data-message-success hidden>
                <h3>发送成功提醒</h3>
                <p>如果邮件客户端已经弹出，请确认发送。为了保险，也建议使用 QQ 邮箱网页版联系我。</p>
                <a class="message-webmail" href="https://mail.qq.com/" target="_blank" rel="noreferrer">打开 QQ 邮箱网页版</a>
                <label>
                    <span>刚刚发送的内容</span>
                    <textarea data-message-copy readonly rows="7"></textarea>
                </label>
                <button type="button" data-message-copy-button>复制留言内容</button>
            </div>
        </section>
    </div>

    <script>
        const buttons = document.querySelectorAll("[data-lang-button]");
        const panels = document.querySelectorAll("[data-lang-panel]");
        const modal = document.querySelector("[data-message-modal]");
        const dialog = document.querySelector("[data-message-dialog]");
        const openMessageButtons = document.querySelectorAll("[data-message-open]");
        const closeMessageButtons = document.querySelectorAll("[data-message-close]");
        const messageForm = document.querySelector("[data-message-form]");
        const messageSuccess = document.querySelector("[data-message-success]");
        const messageCopy = document.querySelector("[data-message-copy]");
        const messageCopyButton = document.querySelector("[data-message-copy-button]");
        let previousFocus = null;

        buttons.forEach((button) => {
            button.addEventListener("click", () => {
                const lang = button.getAttribute("data-lang-button");

                buttons.forEach((item) => item.classList.toggle("active", item === button));
                panels.forEach((panel) => {
                    const active = panel.getAttribute("data-lang-panel") === lang;
                    panel.classList.toggle("active", active);
                    panel.toggleAttribute("hidden", !active);
                });
            });
        });

        const openMessageModal = () => {
            previousFocus = document.activeElement;
            modal?.removeAttribute("hidden");
            document.body.classList.add("message-modal-open");
            messageForm?.removeAttribute("hidden");
            messageSuccess?.setAttribute("hidden", "");
            if (messageCopyButton) messageCopyButton.textContent = "复制留言内容";
            dialog?.querySelector("input")?.focus();
        };

        const closeMessageModal = () => {
            modal?.setAttribute("hidden", "");
            document.body.classList.remove("message-modal-open");
            previousFocus?.focus?.();
        };

        openMessageButtons.forEach((button) => button.addEventListener("click", openMessageModal));
        closeMessageButtons.forEach((button) => button.addEventListener("click", closeMessageModal));

        document.addEventListener("keydown", (event) => {
            if (event.key === "Escape" && !modal?.hasAttribute("hidden")) {
                closeMessageModal();
            }
        });

        messageForm?.addEventListener("submit", (event) => {
            event.preventDefault();
            const formData = new FormData(messageForm);
            const contact = String(formData.get("contact") || "").trim();
            const message = String(formData.get("message") || "").trim();
            const plainMessage = `收件人：zq_eugene@qq.com\n主题：来自个人网站的留言\n\n联系方式：${contact}\n\n留言：\n${message}`;
            const subject = encodeURIComponent("来自个人网站的留言");
            const body = encodeURIComponent(`联系方式：${contact}\n\n留言：\n${message}`);
            window.location.href = `mailto:zq_eugene@qq.com?subject=${subject}&body=${body}`;
            if (messageCopy) messageCopy.value = plainMessage;
            messageForm.setAttribute("hidden", "");
            messageSuccess?.removeAttribute("hidden");
            messageCopyButton?.focus();
        });

        messageCopyButton?.addEventListener("click", async () => {
            const text = messageCopy?.value || "";
            try {
                await navigator.clipboard.writeText(text);
                messageCopyButton.textContent = "已复制";
            } catch {
                messageCopy?.focus();
                messageCopy?.select();
                messageCopyButton.textContent = "请手动复制";
            }
        });
    </script>
</BaseLayout>

<style>
    .home-hero {
        position: relative;
        z-index: 10;
        min-height: 100vh;
        display: flex;
        align-items: center;
        padding: calc(var(--header-height) + 56px) var(--page-x) 64px;
    }

    .home-grid {
        width: min(100%, 1280px);
        margin: 0 auto;
        display: grid;
        grid-template-columns: minmax(280px, 0.84fr) minmax(340px, 1.16fr);
        gap: clamp(38px, 7vw, 92px);
        align-items: center;
    }

    .profile-col h1 {
        display: grid;
        gap: 6px;
        margin: 10px 0 18px;
        color: var(--text-main);
        font-family: var(--font-display);
        font-size: clamp(2.2rem, 9.5cqw, 4.15rem);
        line-height: 0.98;
        letter-spacing: 0;
    }

    .profile-col {
        container-type: inline-size;
    }

    .profile-col h1 small {
        color: var(--text-muted);
        font-size: 0.34em;
        font-weight: 680;
        letter-spacing: 0.08em;
        line-height: 1;
        text-transform: uppercase;
    }

    @supports not (font-size: 1cqw) {
        .profile-col h1 {
            font-size: clamp(2.2rem, 4.6vw, 4.15rem);
        }
    }

    .cinematic-frame {
        width: min(100%, 520px);
        aspect-ratio: 16 / 9;
        margin-bottom: 28px;
        overflow: hidden;
        border: 1px solid var(--border);
        border-radius: 4px;
        background: #111;
        box-shadow: 0 24px 70px rgba(0, 0, 0, 0.52);
    }

    .cinematic-frame img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        filter: saturate(0.82) contrast(1.12) brightness(0.78);
        transition: transform 8s ease, filter 600ms ease;
    }

    .cinematic-frame:hover img {
        transform: scale(1.045);
        filter: saturate(0.9) contrast(1.16) brightness(0.9);
    }

    .role-tags {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: 10px;
        color: var(--text-muted);
        font-family: var(--font-mono);
        font-size: 0.83rem;
        letter-spacing: 0.04em;
    }

    .divider {
        width: 4px;
        height: 4px;
        border-radius: 999px;
        background: currentColor;
        opacity: 0.5;
    }

    .bio-col {
        min-height: 420px;
    }

    .language-tabs {
        display: inline-flex;
        gap: 4px;
        margin-bottom: 30px;
        padding: 4px;
        border: 1px solid var(--border);
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.035);
    }

    .language-tabs button {
        min-width: 58px;
        border: 0;
        border-radius: 999px;
        background: transparent;
        color: var(--text-muted);
        cursor: pointer;
        font: inherit;
        font-size: 0.82rem;
        padding: 7px 14px;
        transition: background 180ms ease, color 180ms ease;
    }

    .language-tabs button:hover,
    .language-tabs button.active {
        background: rgba(255, 255, 255, 0.11);
        color: var(--text-main);
    }

    .lang-content {
        opacity: 0;
        transform: translateY(10px);
        transition: opacity 260ms ease, transform 260ms ease;
    }

    .lang-content.active {
        opacity: 1;
        transform: translateY(0);
    }

    .lang-content p {
        margin: 0 0 1.35rem;
        color: var(--text-muted);
        font-size: var(--text-body);
        line-height: 1.86;
    }

    .lang-content .lead {
        color: var(--text-main);
        font-size: var(--text-lead);
        font-weight: 520;
    }

    .lang-content strong {
        color: var(--text-main);
        font-weight: 650;
    }

    .contact-box {
        display: grid;
        gap: 8px;
        margin-top: 44px;
        padding-top: 28px;
        border-top: 1px solid var(--border);
    }

    .contact-box span {
        color: var(--text-dim);
        font-size: 0.86rem;
    }

    .contact-box a {
        width: fit-content;
        color: var(--text-main);
        font-size: 1.25rem;
        font-weight: 700;
        text-decoration: none;
        transition: color 180ms ease;
    }

    .contact-box a:hover {
        color: var(--accent);
    }

    .contact-actions {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: 14px;
    }

    .message-button,
    .message-form button,
    .message-success button,
    .message-webmail {
        border: 1px solid rgba(255, 94, 0, 0.55);
        border-radius: 4px;
        background: rgba(255, 94, 0, 0.12);
        color: var(--text-main);
        cursor: pointer;
        font: inherit;
        font-size: 0.92rem;
        font-weight: 720;
        padding: 10px 16px;
        text-decoration: none;
        transition: background 180ms ease, border-color 180ms ease, transform 180ms ease;
    }

    .message-button:hover,
    .message-form button:hover,
    .message-success button:hover,
    .message-webmail:hover {
        border-color: var(--accent);
        background: rgba(255, 94, 0, 0.24);
        transform: translateY(-1px);
    }

    .message-modal[hidden] {
        display: none;
    }

    .message-modal {
        position: fixed;
        inset: 0;
        z-index: 160;
        display: grid;
        place-items: center;
        padding: 22px;
    }

    .message-modal__backdrop {
        position: absolute;
        inset: 0;
        background: rgba(0, 0, 0, 0.68);
        backdrop-filter: blur(10px);
    }

    .message-dialog {
        position: relative;
        width: min(100%, 560px);
        padding: clamp(24px, 5vw, 34px);
        border: 1px solid var(--border);
        border-radius: 8px;
        background: rgba(12, 12, 13, 0.96);
        box-shadow: 0 28px 90px rgba(0, 0, 0, 0.48);
    }

    :root[data-theme="light"] .message-dialog {
        background: rgba(250, 248, 243, 0.98);
        box-shadow: 0 24px 70px rgba(32, 28, 22, 0.18);
    }

    .message-dialog h2 {
        margin: 8px 0 10px;
        color: var(--text-main);
        font-family: var(--font-display);
        font-size: var(--title-section);
    }

    .message-dialog p {
        margin: 0 0 22px;
        color: var(--text-muted);
        line-height: 1.72;
    }

    .message-close {
        position: absolute;
        top: 14px;
        right: 14px;
        display: grid;
        place-items: center;
        width: 34px;
        height: 34px;
        border: 1px solid var(--border);
        border-radius: 50%;
        background: var(--bg-panel);
        color: var(--text-muted);
        cursor: pointer;
        font: inherit;
        font-size: 1.35rem;
        line-height: 1;
    }

    .message-close:hover {
        color: var(--text-main);
        border-color: rgba(255, 94, 0, 0.44);
    }

    .message-form,
    .message-success {
        display: grid;
        gap: 16px;
    }

    .message-form[hidden],
    .message-success[hidden] {
        display: none;
    }

    .message-success h3 {
        margin: 0;
        color: var(--text-main);
        font-family: var(--font-display);
        font-size: 1.2rem;
    }

    .message-success p {
        margin: 0;
    }

    .message-webmail,
    .message-success button {
        justify-self: start;
    }

    .message-form label,
    .message-success label {
        display: grid;
        gap: 8px;
        color: var(--text-main);
        font-size: 0.9rem;
        font-weight: 650;
    }

    .message-form input,
    .message-form textarea,
    .message-success textarea {
        width: 100%;
        border: 1px solid var(--border);
        border-radius: 6px;
        background: rgba(255, 255, 255, 0.055);
        color: var(--text-main);
        font: inherit;
        line-height: 1.55;
        padding: 12px 13px;
        resize: vertical;
    }

    .message-form input:focus,
    .message-form textarea:focus,
    .message-success textarea:focus {
        outline: 2px solid rgba(255, 94, 0, 0.38);
        outline-offset: 2px;
        border-color: rgba(255, 94, 0, 0.48);
    }

    .message-form button {
        justify-self: start;
        margin-top: 4px;
    }

    :global(body.message-modal-open) {
        overflow: hidden;
    }

    @media (max-width: 900px) {
        .home-hero {
            align-items: flex-start;
            padding-top: calc(var(--header-height) + 44px);
        }

        .home-grid {
            grid-template-columns: 1fr;
            gap: 42px;
        }

        .cinematic-frame {
            aspect-ratio: 16 / 9;
        }

        .bio-col {
            min-height: 0;
        }
    }
</style>
